{
  "name": "jhx-turborepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "check-types": "turbo run check-types -- --jsx preserve",
    "dev": "turbo run dev",
    "format": "turbo run format",
    "lint": "turbo run lint",
    "validate": "turbo run lint check-types test format",
    "publish-packages": "turbo run lint check-types test format build && changeset version && changeset publish"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.8",
    "typescript": "^5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
