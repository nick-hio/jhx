{
  "name": "jhx-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck -- --jsx preserve",
    "dev": "turbo run dev",
    "format": "turbo run format",
    "lint": "turbo run lint",
    "validate": "turbo run typecheck lint test format"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "turbo": "^2.8.1",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  },
  "pnpm": {
    "overrides": {
      "fastify": "^5.7.4"
    }
  }
}
