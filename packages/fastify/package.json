{
  "name": "@jhxdev/fastify",
  "description": "HTMX helper with type-safe attribute mapping and inline route declaration. Converts props into HTMX `hx-*` attributes for templating.",
  "version": "0.0.0",
  "author": "nick-hio",
  "homepage": "https://github.com/nick-hio/jhx/tree/main/packages/fastify#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nick-hio/jhx.git"
  },
  "keywords": [
    "jhx",
    "htmx",
    "jsx",
    "react",
    "fastify"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": {
        "source": "./src/index.ts",
        "default": "./dist/index.d.ts"
      },
      "import": {
        "source": "./src/index.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "source": "./src/index.ts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "scripts": {
    "build": "pnpm run build:lib && pnpm run build:types",
    "build:lib": "bun ../../configs/esbuild-config/build.js",
    "build:types": "rollup -c rollup.config.js",
    "format": "prettier --write . --list-different",
    "lint": "eslint . --max-warnings 0",
    "prepack": "bun ../../configs/prepack-config/prepack.js",
    "test": "bun test ./tests",
    "test:watch": "bun test --watch ./tests",
    "typecheck": "tsc -p tsconfig.json --jsx preserve",
    "validate": "pnpm run typecheck && pnpm run lint && pnpm run test && pnpm run format"
  },
  "dependencies": {
    "fastify-plugin": "^5.1.0",
    "jhx": "workspace:*"
  },
  "peerDependencies": {
    "fastify": "^5",
    "react": "^17.0.2 || ^18.0.0 || ^19.0.0",
    "react-dom": "^17.0.2 || ^18.0.0 || ^19.0.0"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/prettier-config": "workspace:*",
    "@repo/rollup-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@rollup/plugin-alias": "^5.1.1",
    "@types/bun": "^1.3.1",
    "@types/node": "^24.9.2",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "esbuild": "^0.25.11",
    "eslint": "^9.38.0",
    "prettier": "^3.6.2",
    "rollup": "^4.52.5",
    "typescript": "^5.9.3"
  }
}
